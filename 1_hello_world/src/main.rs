fn main() {
    println!("Hello, world!");
}

// cargo new <dir_name> --name <project_name>
// create a new project
// Use --name only if you want to have different directoy and project names
// Use --vcs=git to override making a new repo

// cargo build
// Compile the project

// cargo run 
// Compile and run

// cargo check 
// Check for errors but dont build an executable 
// Much faster than building a binary 

// cargo build --release 
// Compiles with optimizations